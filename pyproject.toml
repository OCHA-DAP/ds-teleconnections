[project]
name = "ds-teleconnections"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
    "geopandas",
    "requests",
    "scipy",
    "matplotlib",
    "ocha-stratus",
]
